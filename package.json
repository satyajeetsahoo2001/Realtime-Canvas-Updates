{
    "name": "live-multi-canvas-server",
    "version": "1.0.0",
    "main": "server.js",
    "scripts": {
        "start": "npm run build-client && npm run start-server",
    "build-client": "cd client && npm install && npm run build",
    "start-server": "cd server && npm install && node server.js"
    },
    "dependencies": {
      "cors": "^2.8.5",
      "express": "^4.18.2",
      "socket.io": "^4.7.2"
    }
  }